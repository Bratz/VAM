// Throwaway file for testing the defect-fix pipeline's new trajectory logging
// (see tasks/defect-autofix-pipeline-design.md). Safe to delete once the
// pipeline has picked this up and filed a Jira ticket for it.
import React from 'react';

export const DefectFixTrajectoryCheck: React.FC = () => {
  return <div>trajectory check</div>;
};
